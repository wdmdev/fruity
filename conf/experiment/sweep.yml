defaults:
  model: timm_model

program: src/fruity/train.py
method: bayes
metric:
  goal: maximize
  name: val/acc

parameters:
  model:
    optimizer:
      lr:
        min: 0.0001
        max: 0.1
  # epochs:
  #   min: 1
  #   max: 10

command:
  - ${env}
  - ${interpreter}
  - ${program}
  - ${args_no_hyphens}