#@package __global__

datamodule:
  num_workers: max
  batch_size: 64

model:
  net.model_name: convnext_base
  net.lr: 0.0001
  net.weight_decay: 0.001

trainer:
  max_epochs: 100

# Example of how to run
# python src/fruity/train.py experiment=experiment1