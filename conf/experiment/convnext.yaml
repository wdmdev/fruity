#@package __global__

datamodule:
  num_workers: max
  batch_size: 64

model:
  net:
    model_name: convnext_large

  optimizer:
    lr: 0.00001
    weight_decay: 0.0

trainer:
  max_epochs: 100

# Example of how to run
# python src/fruity/train.py experiment=experiment1